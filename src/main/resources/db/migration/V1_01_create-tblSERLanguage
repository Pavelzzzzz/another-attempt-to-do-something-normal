CREATE TABLE IF NOT EXISTS tblSERLanguage (
  `LanguageId` INT NOT NULL AUTO_INCREMENT,
  `ISOCode` VARCHAR(3) NOT NULL,
  PRIMARY KEY (`LanguageId`),
  UNIQUE INDEX `ISOCode_UNIQUE` (`ISOCode` ASC));